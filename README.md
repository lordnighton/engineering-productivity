# Useful aliases
```
alias c="clear"
alias s="git status"
alias l="ls -la"
alias gal="git add ."
alias gpom="git pull origin master"
alias grm="git rebase master"
alias gcaa="git commit -a --amend"
```
