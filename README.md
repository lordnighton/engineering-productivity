# Useful aliases
```
alias c="clear"
alias s="git status"
alias l="ls -la"
alias gal="git add ."
alias gl="git log"
alias gpom="git pull origin master"
alias grm="git rebase master"
alias gcaa="git commit -a --amend"
alias b="git branch"
```
